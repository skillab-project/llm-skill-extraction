services:
  db:
    image: postgres:16-alpine
    container_name: upskilling-recommender-db
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      
    volumes:
      - postgres-data:/var/lib/postgresql/data
      
    ports:
      - "5433:5432"
      
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d labourpulse_db"]
      interval: 10s
      timeout: 5s
      retries: 5
      
    networks:
      - upskilling-recommender-network
    labels:
      io.portainer.accesscontrol.teams: skillab-all

  app:
    build: .
    image: ${DOCKER_REG}${DOCKER_REPO}${APP_NAME}:${DOCKER_TAG}
    container_name: upskilling-recommender-app
    
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8888:8080"
    networks:
      - upskilling-recommender-network
    env_file:
      - .env
    labels:
      io.portainer.accesscontrol.teams: skillab-all
      
      
networks:
  upskilling-recommender-network:
    driver: bridge

volumes:
  postgres-data:
    driver: local
